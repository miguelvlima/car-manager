DATABASE_URL="postgresql://carmanager:carmanager@localhost:5433/carmanager?schema=public"
AUTH_SECRET="replace-with-a-long-random-string"
AUTH_URL="http://localhost:3000"
STORAGE_DRIVER="local"
UPLOAD_DIR="./uploads"

# Só para o primeiro arranque em produção (cria o super-admin se a base estiver vazia)
# BOOTSTRAP_ADMIN_EMAIL="admin@empresa.pt"
# BOOTSTRAP_ADMIN_PASSWORD="password-forte"
# BOOTSTRAP_ADMIN_NAME="Administrador"
